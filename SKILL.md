---
name: cinematic-image-prompt
description: 将简短想法、参考图、脚本片段或商品需求改写成清晰、可执行、空间关系稳定的中文生图提示词。适用于真人电影感、纪实摄影、动漫插画、概念场景、建筑室内和电商产品图；尤其适合用户要求优化构图、机位、焦段倾向、景深、光线、色彩、材质、多人关系、文字或负向约束时。除非用户明确要求生成、渲染或直接出图，否则只交付提示词，不调用生图工具；不要用于视频运镜或时序提示词。
---

# Cinematic Image Prompt

把用户的画面意图整理成一张能被看见、能被构图、能被模型稳定执行的静态图像。优先保证主体、数量、属性、空间关系和视觉重点，再添加摄影与美术细节。不要用摄影术语堆砌替代画面设计。

## 使用前读取

- 每次生成前读取 `references/prompt-architecture.md`，按核心模块组织信息。
- 涉及焦段、透视、景别、构图、光线或色彩时读取 `references/visual-language.md`，避免概念误用。
- 涉及多主体、复杂空间、图中文字、参考图或系列一致性时读取 `references/model-failures.md`。
- 用户要求示例、模板或需要校准输出密度时读取 `references/examples.md`。
- 交付前读取 `references/review-rubric.md` 并内部评分；硬门槛失败或低于 90 分时先修订。
- 用户要求依据或来源时读取 `references/sources.md`。

## 核心原则

### 1. 先锁定画面意图

确定唯一主风格与用途：真人电影感、纪实摄影、动漫插画、概念场景、建筑室内或电商产品。用户没有说明时，按题材合理推断；只有真人写实与插画方向完全无法判断且会改变结果时，才问一次。

明确画面的叙事瞬间：谁或什么是第一视觉重点、正在发生什么、观众应先看到什么、画面要传递何种情绪。不要给出多个互相竞争的主方案。

### 2. 建立主体与属性绑定

逐个主体写清数量、身份、稳定外观、服装或材质、动作、表情和位置。多人或多物体场景必须使用明确关系词，例如“画面左侧”“在其身后半步”“前景右下角”“两人之间留出窄缝”。

每项属性紧贴所属主体，避免先列一堆人物再统一列颜色、服装和动作。重要主体不超过画面可承载的信息量；次要人群用群体特征概括，不逐个展开。

### 3. 先做空间布局，再谈镜头

先确定主体在画框中的位置、尺度、遮挡、留白、视线方向和前中后层次，再选择机位、景别、视场倾向和景深。透视主要由摄影机位置和拍摄距离决定；焦段主要控制固定机位下的视场范围。不要把“广角必然恐慌”“长焦必然压迫”写成硬规则。

焦段数值只在能帮助模型或用户明确视觉结果时使用，并同时写出可见效果，例如“近距离广角，前景略有尺度夸张、背景快速缩小”。不要只写 `24mm`、`f/1.4` 等孤立参数。

### 4. 让光线可落地

光线至少说明主光来源、方向、软硬和作用；必要时补充环境填充、轮廓光或实景光源。灯光必须与场景中的窗、太阳、灯具、火焰或屏幕相容。没有必要时，不强行填满主光、辅光、特效光三个槽位。

区分色彩与影调：色彩写主色、辅色、冷暖关系和饱和度；影调写明暗分布、对比、黑位与高光过渡。避免同时写“低对比”和“硬光强烈深黑阴影”等未经解释的冲突要求。

### 5. 只保留有功能的细节

材质、纹理、天气、颗粒、锐度和后期风格必须服务主体识别、空间层次或情绪。不要默认添加“8K、超高清、杰作、极致细节、HDR、锐利”等无差别质量词。避免用“胶片感”代替具体的颗粒、色偏、反差和高光表现。

### 6. 使用针对性负向约束

只写当前画面最可能出现的错误，例如多余肢体、主体重复、属性串色、透视断裂、文字拼写错误、塑料皮肤、过度锐化、灰雾、脏色、背景抢主体。正向提示词已经明确的内容，不要再用几十个通用禁词反向重复。

### 7. 区分写提示词与直接出图

用户只说“写、优化、改写、整理提示词”时，只输出提示词。只有用户明确说“生成图片”“直接出图”“渲染”时才调用生图工具。存在参考图时，先说明每张参考图负责身份、构图、材质、色彩或姿态中的哪一项，不能只写“参考附件”。

## 工作流

1. 提取不可改变的信息：主体、数量、动作、场景、文字、画幅和用户指定风格。
2. 选择一个叙事瞬间和唯一视觉中心。
3. 写内部空间草图：左右、前后、高低、遮挡、视线和留白。
4. 选择机位、景别、视场倾向、焦点与景深，检查是否互相兼容。
5. 设计有动机的光源、色彩关系、影调和材质。
6. 合并成自然语言正向提示词；把硬要求放在前半段。
7. 添加少量针对性负向约束。
8. 按审核表评分；失败则改写后再交付。

除非用户要求教学说明，不展示步骤 1–8 的内部草图、取舍过程或评分，只交付通过后的结果。

## 默认输出

默认输出可直接复制的中文文本，不展示内部推理：

```text
【正向提示词】
媒介与主风格；主体、数量、身份与关键属性；动作、表情与主体关系；场景与空间布局；构图与视觉中心；摄影机位置、景别、视场/镜头表现与焦点；光源结构；色彩与影调；材质与成像质感；画幅比例。

【负向约束】
仅列本画面高风险错误。
```

用户要求结构化拆解时，改用以下模块；只写适用项，不留空、不用“无”凑数：

```text
【用途与主风格】
【画幅与视觉中心】
【主体与数量】
【动作、表情与关系】
【场景与空间布局】
【机位与视点】
【景别、视场与透视表现】
【焦点与景深】
【光源结构】
【色彩与影调】
【材质与成像质感】
【文字内容】（仅在画面需要文字时）
【参考图职责】（仅在存在参考图时）
【负向约束】
```

多主体时，在同一提示词中依次写“主体一、主体二……”，每个主体的属性、动作和位置紧邻书写；共享场景、光线与色彩只写一次。

## 风格适配

- **真人电影感/纪实摄影**：强调可信皮肤、自然材质、摄影机位置、真实光源、适度颗粒与高光过渡；避免蜡像皮肤、过锐和虚假 HDR。
- **动漫插画**：把摄影参数翻译成画面语言，强调线条、形状、色块、透视、笔触或赛璐璐层次；不要强塞真实相机型号。
- **电商产品**：优先产品形状、比例、包装文字、材质反射、干净背景和可控阴影；氛围不能牺牲商品识别。
- **建筑/室内**：优先空间尺度、垂直线、消失点、材质、采光方向与人作为尺度参照；避免不可能结构。
- **概念场景**：优先世界规则、尺度参照、空间层次、天气与大形关系；细节服从剪影和视觉中心。

## 交付前硬门槛

- 只有一个明确的第一视觉重点。
- 主体数量、属性和位置明确；多主体没有串色或归属歧义。
- 机位、景别、空间与透视描述不冲突。
- 光源有来源、有方向，并与场景相容。
- 色彩、影调、材质和风格彼此一致。
- 没有为了填字段制造不存在的天气、辅光、特效光或胶片型号。
- 没有无功能的质量词堆砌，也没有过长通用负面词表。
- 用户未要求出图时，没有调用生图工具。
