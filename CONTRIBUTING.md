# 参与贡献

欢迎提交真实生成案例、失败样本、规则修正与平台安装验证。贡献应帮助其他人复现结果，而不是只增加形容词或未经验证的参数。

## 提交 Case

请至少提供：

1. 原始需求和使用的完整提示词。
2. 模型、版本、画幅和可复现的关键设置。
3. 生成结果或关键帧；确保你有权公开素材。
4. 哪些部分成功、哪些部分失败。
5. 从案例中提炼出的可迁移规则，而不是只描述这一张图。

不要提交隐私素材、未经许可的人脸、受限商业资产或不能公开再分发的内容。

## 提交规则修正

说明现有规则在什么请求中失效，附最小复现请求、实际输出和期望行为。一个案例不足以成为绝对规则时，应写成条件或建议。

## 修改 Skill

- `SKILL.md` 只保留高频、会改变决策的核心要求。
- 大型示例、专业资料和平台细节分别放入 `cases/`、`references/` 和 `docs/`。
- 新增引用优先使用教材、论文或官方文档。
- 修改后运行 Skill 校验，并用至少一个正常用例和一个失败边界用例测试。
